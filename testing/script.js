document.querySelector(".options").addEventListener('click', function (event) {
    //event.target.childNodes[1].submit();
    console.log(event.target.innerHTML);
    //event.target.childNodes[1]
    
});